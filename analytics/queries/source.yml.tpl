type: postgres
details:
  database: ${LATITUDE__POSTGRES_DB}
  user: ${LATITUDE__POSTGRES_USER}
  password: ${LATITUDE__POSTGRES_PWD}
  host: ${LATITUDE__POSTGRES_HOST}
  port: ${LATITUDE__POSTGRES_PORT}
  schema: ${LATITUDE__POSTGRES_SCHEMA}
  ssl: false
